import 'package:flutter/material.dart';

class MovieSecurityContent extends StatelessWidget {
  const MovieSecurityContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
